<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Camera is the only mandatory permission. It is declared as not required
         at hardware level so the app can still be installed on camera-less
         devices (they simply use gallery/manual scanning). -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature
        android:name="android.hardware.camera.any"
        android:required="false" />

    <!-- Vibration for scan feedback (no runtime prompt). -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Internet is used ONLY for: Safe Browsing URL checks, ads, billing and
         Play Integrity. All traffic is forced through HTTPS/TLS via the
         Network Security Config below. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Reading images from the gallery: scoped, media-only permission on 13+.
         Older devices fall back to the Photo Picker which needs no permission. -->
    <uses-permission
        android:name="android.permission.READ_MEDIA_IMAGES"
        android:minSdkVersion="33" />

    <!-- Ad id permission required by Play services ads on Android 13+. -->
    <uses-permission android:name="com.google.android.gms.permission.AD_ID" />

    <!-- Explicitly NOT requested: ACCESS_FINE_LOCATION, RECORD_AUDIO,
         READ_CONTACTS, WRITE_EXTERNAL_STORAGE. Wi-Fi/contact/calendar actions
         are handed off to the system via Intents, never by holding the
         permission ourselves. -->

    <application
        android:name=".NextQrApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:enableOnBackInvokedCallback="true"
        android:theme="@style/Theme.NextQrScanner"
        tools:targetApi="34">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/Theme.NextQrScanner.Splash"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Deep link entry point used by the home-screen widget. -->
            <intent-filter>
                <action android:name="com.nextqr.scanner.ACTION_QUICK_SCAN" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!-- AdMob application id — injected at build time, never hard-coded. -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="${admobAppId}" />

        <!-- Home-screen quick-scan widget. -->
        <receiver
            android:name=".widget.QuickScanWidgetProvider"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/quick_scan_widget_info" />
        </receiver>

        <!-- FileProvider for sharing generated QR images / exported files. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Hilt/WorkManager: disable default initializer, use on-demand. -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>
    </application>
</manifest>
